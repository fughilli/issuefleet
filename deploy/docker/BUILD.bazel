# Runner targets for the homelab stack. These are deliberate `docker ...`
# wrappers, not hermetic rules_oci builds: the image's apt layer can't be
# built hermetically without heavy machinery, and wrappers keep
# `bazel build //...` green on machines without docker (same discipline as
# Nix targets). They need docker on PATH at *run* time only.
#
#   bazel run //deploy/docker:image   # build ghcr.io/fughilli/issuefleet:dev
#   bazel run //deploy/docker:up      # compose up -d (daemon + tailscale funnel)
#   bazel run //deploy/docker:down    # compose down

sh_binary(
    name = "image",
    srcs = ["image.sh"],
    data = ["Dockerfile"],
)

sh_binary(
    name = "up",
    srcs = ["up.sh"],
    data = [
        "docker-compose.yml",
        "env.sh",
        "funnel.json",
    ],
)

# The outer deploy loop: keep the stack up AND current. Polls ghcr.io (or, in
# source mode, origin/main) and recreates the daemon onto each new build.
sh_binary(
    name = "watch",
    srcs = ["watch.sh"],
    data = [
        "docker-compose.yml",
        "env.sh",
        "funnel.json",
    ],
)

sh_binary(
    name = "down",
    srcs = ["down.sh"],
    data = [
        "docker-compose.yml",
        "env.sh",
    ],
)

sh_binary(
    name = "doctor",
    srcs = ["doctor.sh"],
    data = [
        "docker-compose.yml",
        "env.sh",
    ],
)

sh_binary(
    name = "status",
    srcs = ["status.sh"],
    data = [
        "docker-compose.yml",
        "env.sh",
    ],
)

sh_binary(
    name = "logs",
    srcs = ["logs.sh"],
    data = [
        "docker-compose.yml",
        "env.sh",
    ],
)

sh_binary(
    name = "attach",
    srcs = ["attach.sh"],
    data = [
        "docker-compose.yml",
        "env.sh",
    ],
)

sh_binary(
    name = "stop",
    srcs = ["stop.sh"],
    data = [
        "docker-compose.yml",
        "env.sh",
    ],
)
